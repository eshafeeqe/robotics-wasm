[package]
name = "robotics-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] # Crucial: tells Rust to build a .wasm file

[dependencies]
wasm-bindgen = "0.2" # The bridge between Rust and JavaScript
serde = { version = "1.0", features = ["derive"] }
serde-wasm-bindgen = "0.6"